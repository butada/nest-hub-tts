services:
  nest-hub-tts:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      MEDIA_DIR: /data/media
      BATCH_DB_PATH: /data/jobs.sqlite3
      MEDIA_PUBLIC_BASE_URL: http://192.168.50.7:8080
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
